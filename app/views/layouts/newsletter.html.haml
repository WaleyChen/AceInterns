!!!
%html
  %head
    %title AceInterns
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    %meta{:name => "description", :content => "AceInterns - We connect you with awesome internships"}
  %body
    = yield :nav
    #newsletter-center
      = yield :center_panel
    #newsletter-left-panel.container.pull-left
      = yield :left_panel
    #newsletter-right-panel.container.pull-right.fixed
      = yield :right_panel
    #bottom-panel
      = yield :bottom_panel
  = yield :javascript
